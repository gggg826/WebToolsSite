# IT365 工具箱 - 代码架构设计

## 1. 技术栈选型

### 1.1 核心技术
- **前端框架**：React 18+ (可替换为 Vue 3 或 Svelte)
- **构建工具**：Vite 5+
- **路由管理**：React Router v6
- **状态管理**：Zustand (轻量级) 或 Context API
- **UI 组件库**：Tailwind CSS + shadcn/ui
- **国际化**：react-i18next
- **工具库**：lodash-es、dayjs

### 1.2 开发工具
- **包管理器**：pnpm
- **代码规范**：ESLint + Prettier
- **类型检查**：TypeScript 5+
- **版本控制**：Git
- **部署平台**：Vercel / Cloudflare Pages / GitHub Pages

## 2. 项目目录结构

```
it365-toolbox/
├── public/                      # 静态资源
│   ├── favicon.ico
│   ├── robots.txt
│   └── ads.txt                  # 广告验证文件
│
├── src/
│   ├── assets/                  # 资源文件
│   │   ├── icons/              # 工具图标
│   │   ├── images/             # 图片资源
│   │   └── styles/             # 全局样式
│   │       ├── index.css       # 全局样式入口
│   │       └── variables.css   # CSS 变量
│   │
│   ├── components/             # 公共组件
│   │   ├── layout/            # 布局组件
│   │   │   ├── Header.tsx     # 顶部导航
│   │   │   ├── Footer.tsx     # 底部
│   │   │   ├── Sidebar.tsx    # 侧边栏
│   │   │   └── Container.tsx  # 容器组件
│   │   │
│   │   ├── common/            # 通用组件
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Toast.tsx
│   │   │   └── Loading.tsx
│   │   │
│   │   ├── tool/              # 工具相关组件
│   │   │   ├── ToolCard.tsx   # 工具卡片
│   │   │   ├── ToolGrid.tsx   # 工具网格
│   │   │   ├── ToolSearch.tsx # 工具搜索
│   │   │   ├── ToolFilter.tsx # 工具筛选
│   │   │   └── ToolWrapper.tsx # 工具容器包装器
│   │   │
│   │   └── ad/                # 广告组件
│   │       ├── AdBanner.tsx   # 横幅广告
│   │       ├── AdSidebar.tsx  # 侧边广告
│   │       └── AdInContent.tsx # 内容中广告
│   │
│   ├── tools/                  # 工具模块（核心）
│   │   ├── index.ts           # 工具注册中心
│   │   ├── types.ts           # 工具类型定义
│   │   │
│   │   ├── text/              # 文本处理类工具
│   │   │   ├── index.ts       # 分类导出
│   │   │   ├── json-formatter/
│   │   │   │   ├── index.tsx  # 工具主文件
│   │   │   │   ├── config.ts  # 工具配置
│   │   │   │   └── utils.ts   # 工具逻辑
│   │   │   │
│   │   │   ├── base64/
│   │   │   │   ├── index.tsx
│   │   │   │   ├── config.ts
│   │   │   │   └── utils.ts
│   │   │   │
│   │   │   └── ...
│   │   │
│   │   ├── dev/               # 开发工具类
│   │   │   ├── index.ts
│   │   │   ├── uuid-generator/
│   │   │   ├── timestamp-converter/
│   │   │   └── ...
│   │   │
│   │   ├── image/             # 图片处理类
│   │   │   ├── index.ts
│   │   │   ├── qrcode-generator/
│   │   │   └── ...
│   │   │
│   │   └── ...                # 其他分类
│   │
│   ├── pages/                  # 页面组件
│   │   ├── Home.tsx           # 主页
│   │   ├── ToolPage.tsx       # 工具页面
│   │   ├── About.tsx          # 关于页面
│   │   └── NotFound.tsx       # 404 页面
│   │
│   ├── hooks/                  # 自定义 Hooks
│   │   ├── useLanguage.ts     # 语言切换
│   │   ├── useLocalStorage.ts # 本地存储
│   │   ├── useTheme.ts        # 主题切换
│   │   └── useSEO.ts          # SEO 设置
│   │
│   ├── stores/                 # 状态管理
│   │   ├── languageStore.ts   # 语言状态
│   │   ├── themeStore.ts      # 主题状态
│   │   └── configStore.ts     # 配置状态
│   │
│   ├── i18n/                   # 国际化
│   │   ├── index.ts           # i18n 配置
│   │   ├── locales/           # 语言文件
│   │   │   ├── zh-CN.json     # 简体中文
│   │   │   ├── en-US.json     # 英文
│   │   │   └── ...
│   │   │
│   │   └── tools/             # 工具多语言
│   │       ├── zh-CN/
│   │       │   ├── text.json
│   │       │   └── dev.json
│   │       │
│   │       └── en-US/
│   │           ├── text.json
│   │           └── dev.json
│   │
│   ├── utils/                  # 工具函数
│   │   ├── common.ts          # 通用工具函数
│   │   ├── validator.ts       # 验证函数
│   │   ├── formatter.ts       # 格式化函数
│   │   └── constants.ts       # 常量定义
│   │
│   ├── config/                 # 配置文件
│   │   ├── site.ts            # 网站配置
│   │   ├── ads.ts             # 广告配置
│   │   ├── seo.ts             # SEO 配置
│   │   └── theme.ts           # 主题配置
│   │
│   ├── App.tsx                 # 应用根组件
│   ├── main.tsx               # 应用入口
│   └── router.tsx             # 路由配置
│
├── docs/                       # 文档
│   ├── 需求文档.md
│   ├── 代码架构设计.md
│   ├── 开发计划.md
│   └── 工具编写规范.md
│
├── .env.example               # 环境变量示例
├── .eslintrc.cjs             # ESLint 配置
├── .prettierrc               # Prettier 配置
├── tsconfig.json             # TypeScript 配置
├── vite.config.ts            # Vite 配置
├── package.json              # 项目依赖
└── README.md                 # 项目说明
```

## 3. 核心模块设计

### 3.1 工具注册系统

#### 3.1.1 工具配置接口（types.ts）

```typescript
// 工具分类
export enum ToolCategory {
  TEXT = 'text',
  DEV = 'dev',
  IMAGE = 'image',
  DATA = 'data',
  OTHER = 'other',
}

// 工具元数据
export interface ToolMetadata {
  id: string;                    // 工具唯一标识
  name: string;                  // 工具名称（i18n key）
  description: string;           // 工具描述（i18n key）
  category: ToolCategory;        // 工具分类
  icon: string;                  // 图标（可用 emoji 或图标组件名）
  keywords: string[];            // 搜索关键词（i18n key）
  author?: string;               // 作者
  version?: string;              // 版本
  tags?: string[];               // 标签
}

// 工具组件接口
export interface ToolComponent {
  metadata: ToolMetadata;        // 工具元数据
  component: React.LazyExoticComponent<React.ComponentType>; // 懒加载组件
}

// 工具Props接口
export interface ToolProps {
  // 可扩展的工具通用props
}
```

#### 3.1.2 工具注册中心（tools/index.ts）

```typescript
import { ToolComponent, ToolCategory } from './types';
import { lazy } from 'react';

// 自动导入所有工具
const toolModules = import.meta.glob('./*/*/config.ts', { eager: true });

// 工具注册表
export const toolRegistry: ToolComponent[] = [];

// 自动注册工具
Object.entries(toolModules).forEach(([path, module]: [string, any]) => {
  const metadata = module.default;
  const componentPath = path.replace('/config.ts', '/index.tsx');

  toolRegistry.push({
    metadata,
    component: lazy(() => import(/* @vite-ignore */ componentPath)),
  });
});

// 按分类获取工具
export const getToolsByCategory = (category: ToolCategory) => {
  return toolRegistry.filter(tool => tool.metadata.category === category);
};

// 根据ID获取工具
export const getToolById = (id: string) => {
  return toolRegistry.find(tool => tool.metadata.id === id);
};

// 搜索工具
export const searchTools = (keyword: string, locale: string) => {
  // 实现搜索逻辑（包含多语言）
};

// 导出所有工具
export default toolRegistry;
```

### 3.2 国际化系统

#### 3.2.1 i18n 配置（i18n/index.ts）

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';

// 导入公共语言文件
import zhCN from './locales/zh-CN.json';
import enUS from './locales/en-US.json';

// 动态导入工具语言文件
const toolLocales = import.meta.glob('./tools/*/*.json', { eager: true });

// 合并语言资源
const resources = {
  'zh-CN': { translation: { ...zhCN, tools: {} } },
  'en-US': { translation: { ...enUS, tools: {} } },
};

// 处理工具语言文件
Object.entries(toolLocales).forEach(([path, module]) => {
  const [_, locale, category] = path.match(/\/(zh-CN|en-US)\/(.+)\.json/) || [];
  if (locale && category) {
    resources[locale].translation.tools[category] = module;
  }
});

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources,
    fallbackLng: 'zh-CN',
    interpolation: {
      escapeValue: false,
    },
  });

export default i18n;
```

### 3.3 路由系统

#### 3.3.1 路由配置（router.tsx）

```typescript
import { createBrowserRouter } from 'react-router-dom';
import Layout from './components/layout/Layout';
import Home from './pages/Home';
import ToolPage from './pages/ToolPage';
import About from './pages/About';
import NotFound from './pages/NotFound';

export const router = createBrowserRouter([
  {
    path: '/',
    element: <Layout />,
    children: [
      {
        index: true,
        element: <Home />,
      },
      {
        path: 'tool/:toolId',
        element: <ToolPage />,
      },
      {
        path: 'about',
        element: <About />,
      },
      {
        path: '*',
        element: <NotFound />,
      },
    ],
  },
]);
```

### 3.4 工具容器组件

#### 3.4.1 ToolWrapper 组件

```typescript
// components/tool/ToolWrapper.tsx
import { Suspense } from 'react';
import { useTranslation } from 'react-i18next';
import { ToolComponent } from '@/tools/types';
import Loading from '@/components/common/Loading';
import AdBanner from '@/components/ad/AdBanner';

interface ToolWrapperProps {
  tool: ToolComponent;
}

export default function ToolWrapper({ tool }: ToolWrapperProps) {
  const { t } = useTranslation();
  const { metadata, component: Component } = tool;

  return (
    <div className="tool-wrapper">
      {/* 工具头部 */}
      <div className="tool-header">
        <h1>{t(metadata.name)}</h1>
        <p>{t(metadata.description)}</p>
      </div>

      {/* 顶部广告 */}
      <AdBanner position="tool-top" />

      {/* 工具内容区 */}
      <div className="tool-content">
        <Suspense fallback={<Loading />}>
          <Component />
        </Suspense>
      </div>

      {/* 底部广告 */}
      <AdBanner position="tool-bottom" />
    </div>
  );
}
```

### 3.5 广告系统

#### 3.5.1 广告配置（config/ads.ts）

```typescript
export interface AdConfig {
  enabled: boolean;
  provider: 'google' | 'baidu' | 'custom';
  slots: {
    [key: string]: {
      id: string;
      format?: string;
      responsive?: boolean;
    };
  };
}

export const adConfig: AdConfig = {
  enabled: true,
  provider: 'google',
  slots: {
    'home-top': {
      id: 'ca-pub-xxxxx',
      format: 'horizontal',
      responsive: true,
    },
    'tool-top': {
      id: 'ca-pub-xxxxx',
      format: 'horizontal',
      responsive: true,
    },
    'sidebar': {
      id: 'ca-pub-xxxxx',
      format: 'vertical',
    },
  },
};
```

#### 3.5.2 广告组件（components/ad/AdBanner.tsx）

```typescript
import { useEffect, useRef } from 'react';
import { adConfig } from '@/config/ads';

interface AdBannerProps {
  position: string;
}

export default function AdBanner({ position }: AdBannerProps) {
  const adRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!adConfig.enabled) return;

    const slot = adConfig.slots[position];
    if (!slot) return;

    // 加载广告脚本
    try {
      (window as any).adsbygoogle = (window as any).adsbygoogle || [];
      (window as any).adsbygoogle.push({});
    } catch (error) {
      console.error('Ad loading error:', error);
    }
  }, [position]);

  if (!adConfig.enabled) return null;

  const slot = adConfig.slots[position];
  if (!slot) return null;

  return (
    <div className="ad-container" ref={adRef}>
      <ins
        className="adsbygoogle"
        style={{ display: 'block' }}
        data-ad-client={slot.id}
        data-ad-slot={slot.id}
        data-ad-format={slot.format}
        data-full-width-responsive={slot.responsive}
      />
    </div>
  );
}
```

## 4. 数据流设计

### 4.1 状态管理

```
用户操作
   ↓
Store (Zustand/Context)
   ↓
组件更新
   ↓
localStorage 持久化
```

### 4.2 工具加载流程

```
用户点击工具卡片
   ↓
路由跳转 /tool/:toolId
   ↓
根据 toolId 从注册表获取工具
   ↓
懒加载工具组件
   ↓
渲染工具页面
```

### 4.3 语言切换流程

```
用户点击语言切换按钮
   ↓
更新 i18n 语言设置
   ↓
触发组件重新渲染
   ↓
保存语言偏好到 localStorage
```

## 5. 性能优化策略

### 5.1 代码分割
- 路由级代码分割（React.lazy）
- 工具组件懒加载
- 第三方库按需加载

### 5.2 资源优化
- 图片懒加载
- 静态资源 CDN 加速
- Gzip/Brotli 压缩

### 5.3 渲染优化
- 虚拟滚动（工具列表）
- React.memo 防止不必要的重渲染
- 防抖/节流（搜索、筛选）

### 5.4 缓存策略
- Service Worker 缓存静态资源
- localStorage 缓存用户偏好
- HTTP 缓存头设置

## 6. SEO 优化

### 6.1 元标签管理
- 使用 react-helmet-async
- 每个页面独立的 title、description、keywords

### 6.2 SSR/SSG
- 使用 Vite SSR 或 Next.js 实现
- 生成静态 HTML

### 6.3 Sitemap 和 Robots
- 自动生成 sitemap.xml
- 配置 robots.txt

## 7. 部署架构

### 7.1 构建流程
```
Git Push
   ↓
CI/CD 触发（GitHub Actions）
   ↓
npm run build
   ↓
生成静态文件（dist/）
   ↓
部署到 Vercel/Cloudflare Pages
```

### 7.2 CDN 配置
- 静态资源上传到 CDN
- 配置缓存策略
- 启用 HTTPS

## 8. 安全考虑

### 8.1 XSS 防护
- 用户输入过滤
- CSP（Content Security Policy）
- DOMPurify 净化 HTML

### 8.2 数据隐私
- 所有计算在客户端执行
- 不上传用户数据
- 隐私政策说明

## 9. 扩展性设计

### 9.1 插件化工具
- 工具完全独立
- 统一接口规范
- 支持第三方工具接入

### 9.2 主题系统
- CSS 变量定义主题
- 支持亮色/暗色模式
- 可自定义主题

### 9.3 多语言扩展
- 易于添加新语言
- 语言包分离
- 社区翻译支持

## 10. 监控与分析

### 10.1 性能监控
- Web Vitals 监控
- 错误日志收集（Sentry）

### 10.2 用户分析
- Google Analytics / 百度统计
- 用户行为分析
- 工具使用统计

---

**架构原则**：
1. **模块化**：高内聚、低耦合
2. **可扩展**：易于添加新功能
3. **高性能**：快速加载和响应
4. **易维护**：清晰的代码结构和文档
5. **用户友好**：良好的交互体验
